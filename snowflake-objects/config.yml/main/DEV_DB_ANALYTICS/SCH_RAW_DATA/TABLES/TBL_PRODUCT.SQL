---version: 0
CREATE OR ALTER TABLE ${MY_DB}.SCH_RAW_DATA.TBL_PRODUCT(
product_id NUMBER,
product_name VARCHAR
);

---rollback:
DROP TABLE IF EXISTS ${MY_DB}.SCH_RAW_DATA.TBL_PRODUCT;

---verify:
SELECT * FROM ${MY_DB}.SCH_RAW_DATA.TBL_PRODUCT LIMIT 1;
